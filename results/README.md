# Generated measurements

This directory is intentionally shipped without benchmark numbers. Running
`scripts/benchmark_serving.py` on the required NVIDIA GPU creates the raw CSV,
metadata, and server logs here. Running `scripts/plot_serving_results.py`
creates the summary CSV and LaTeX table.

Do not fill these files with estimated or illustrative values: the assignment
asks for empirical measurements.

